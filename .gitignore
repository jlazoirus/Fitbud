# Claude Code local (versionar solo los subagentes del repo; lo demás es local)
.claude/*
!.claude/agents/

# OS
.DS_Store

# Dependencies / build (por si se usa Vercel CLI)
node_modules/
.vercel/

# Archivos de auditoría y capturas locales
assets/ux-audit-*/

# Análisis y materiales locales (no versionar)
ANALISIS_ARQUITECTURA_NUTRICION_FITIA*.md

# Nunca subir secretos
.env
.env.local
*.key
